{
  "name": "feedback-plugin-kit",
  "version": "0.29.0",
  "private": true,
  "type": "module",
  "description": "Shared UI kit for cracklydisc's fee[dB]ack plugins. Vendored, not depended on.",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
